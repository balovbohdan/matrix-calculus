import { Matrix } from '../Matrix';
/**
 * Creates matrix with the same unit all over the grid.
 */
export declare class UnitMatrixFactory {
    static create(rowsNum: number, colsNum: number, unit: number): Matrix;
}
