import { Matrix } from '../Matrix';
export declare class TermwiseMultiplicator {
    static multiply(multiplicand: Matrix, factor: Matrix | number): Matrix;
    private static matrixToNumber;
    private static matrixToMatrix;
}
